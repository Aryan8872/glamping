export interface Experience {
    id: number;
    title: string;
    slug: string;
    icon?: string;
    imageUrl?: string;
    description?: string;
    isActive: boolean;
    createdAt?: string;
    updatedAt?: string;
}
